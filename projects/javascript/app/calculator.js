module.exports = class Calculator {
    add(x,y){ // returning subtraction of two numbers.
        return x + y;
    }
    subtract(x,y){ // returning subtraction of two numbers.
        return x - y;
    }
    multiply(x,y){ // returning subtraction of two numbers.
        return x * y;
    }
}